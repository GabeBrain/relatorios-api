project_id = "mxinpvcqzbfbzjodhgtz"

[functions.analyze-slide]
verify_jwt = false

[functions.socio-proxy]
# A autenticação é o Bearer token da API Geobrain, validado pelo serviço upstream.
verify_jwt = false

[functions.rais-employees-report]
# O token de entrada é o Bearer do GeoBrain, não um JWT do Supabase.
verify_jwt = false

[functions.validacao-fechamento-approvals]
# O token de entrada é o Bearer do GeoBrain, validado pela própria função.
verify_jwt = false

[functions.empresas-report]
# O token de entrada é o Bearer do GeoBrain, validado pela própria função.
verify_jwt = false

[functions.empresas-materialize-pilot]
# Função interna: autenticada pelo segredo EMPRESAS_MATERIALIZER_SECRET no header próprio.
verify_jwt = false

[functions.empresas-materialize-request]
# Solicitação pública: sem Supabase Auth e sem login; protegida por limites por IP (hash) e globais.
verify_jwt = false
